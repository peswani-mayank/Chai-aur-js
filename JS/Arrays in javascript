//in this file we will learn about arrays
//let's start

//array is a non-primitive datatype in javascript. it is also called reference datatype because it stores the reference of the value in the memory. when we assign an array to a variable, we are actually storing the reference of the value in the variable. that's why when we change the value of the array, it will change the value of the variable as well.   

//array: collection of similar or different datatypes. it is a non-primitive datatype in javascript. it is also called reference datatype because it stores the reference of the value in the memory. when we assign an array to a variable, we are actually storing the reference of the value in the variable. that's why when we change the value of the array, it will change the value of the variable as well.
//method 1 declare array
const arr=[1,2,3,4,5];
//console.log(arr); // [1, 2, 3, 4, 5]

//method 2 declare array
const arr1=new Array(1,2,3,4,5);
//console.log(arr1); // [1, 2, 3, 4, 5]

 //array methods

arr.push(6); //it will add the element at the end of the array
// console.log(arr); // [1, 2, 3, 4, 5, 6]

 arr.pop(); //it will remove the last element of the array
// console.log(arr); // [1, 2, 3, 4, 5]

arr.shift(); //it will remove the first element of the array
//console.log(arr); // [2, 3, 4, 5]

arr.unshift(1); //it will add the element at the beginning of the array

//console.log(arr); // [1, 2, 3, 4, 5]

// console.log(arr.includes(8));
 //console.log(arr.indexOf(3));


const newArr=arr.join(); ///isne purane array ko string type ka bna diya or wrap bi kr diya h 
// console.log(arr);
// console.log(newArr);
// console.log(typeof newArr);



//slice, splice

//console.log("A", arr);

const myn1=arr.slice(1,3); ///it will not include last digit [2,3]

//console.log(myn1);
//console.log("B", arr);


const myn2=arr.splice(1,3); //it will include last digit [2,3,4] 
//console.log("C", arr);

//or splice m jo portion remove kiya wo nhi print krega final ye original array ko bi change krdega 
//eg: original array was [1,2,3,4,5]
//splice m apn n index 1 to 3 tk remove kiya ot fr jb fr s array print krne gye to isne jo elment 1 to 3 tk h usko nhi print kiye or baaki elements ko print krdiya or esa slice m nhi hota h slice original array ko nhi change krta h 

//console.log(myn2);

const marvel_heroes=["thor","ironman","spiderman"];
const dc_heroes=["superman","flash","batman"];

//marvel_heroes.push(dc_heroes)
//console.log(marvel_heroes) //isme array k andar array aajyega eg: marvel_heroes array m dc_heroes bi aajyega

//[ 'thor', 'ironman', 'spiderman', [ 'superman', 'flash', 'batman' ] ]
//ab yha pr jodc_heroes h wo single element h marvel_heroes ka jiski index no. h 3

//agar ab apn ko dc_heroes ka elemnt ko access krna h marvel_heores m s to ese krege

//console.log(marvel_heroes[3][1]);


const allHeroes=marvel_heroes.concat(dc_heroes)
//console.log(allHeroes); 
// //[ 'thor', 'ironman', 'spiderman', 'superman', 'flash', 'batman' ]

//concat h wo new array return krta h or jo push h wo existing array pr hi kaam krta h wo new array nhi return krta h



//another method is spread method
const allHero1=[...marvel_heroes,...dc_heroes]
//console.log(allHero1)

const myarr1=[1,2,3,[4,5,6],7,[6,7,[4,5]]]
const myarr2=myarr1.flat(2)
//ye tb use krte h jab array k andr array or apn ko ek hi single array bnana h ho tb flat use krta h 
 //or parameter m 2 depth h kitne depth tk array ko flat krna h or agar apn ko count nhi krna depth to apn Infinity likh skte h or pura array apne ap flat krdega 

//console.log(myarr2)

//console.log(Array.isArray("Hitesh"))
 //it  will check the given parameter is an array or not 

//console.log(Array.from("Hitesh")) 
// //it will convert the given parameter into array 

//console.log(Array.from({name:"hitesh"})) //it iss interensting case
// //it will give empty array because here we need to clarify that of what array we need like we need array or keys or we need array of values

let score1=100;
let score2=200;
let score3=300;
console.log(Array.of(score1,score2,score3)) //it will create the new array of given variables 
