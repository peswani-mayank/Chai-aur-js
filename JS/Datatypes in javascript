//in this file we will define the data types that we will use in our tests

"use strict"; //it will treat all JS code in newer version

// javascript ka engine browser m chupa hua h

//number
//string
//boolean
//bigint
//undefined : ye tb hoga sb apn variable declare kiya h but usko koi value nhi assign ki h 
//eg. let name; //undefined

//null
//object: a collection of key value pairs of the data
//symbol: a unique and primitive identifier

//when we check the type of null variable it will give us object datatype or null is object. we can also say it is bug in javascript that's why it return objects as datatype of null. null is actually a primitive datatype


// to check the type of any variable we can use typeof operator. it will return the datatype of the variable

//eg let name = "John"; //string
//console.log(typeof name); //string



//datatype conversion: we can convert one datatype to another datatype using type conversion. there are two types of type conversion: implicit and explicit

let num = 10; //number
let str = "20"; //string
 let num1=Number(str); //explicit conversion
//console.log(num1); 

//console.log(typeof num1); //number

let num2="33abc"
//console.log(Number(num2)); //NaN: Not a Number
//because num2 is not proper number so it will return NaN. NaN is a special value in javascript which indicates that the value is not a number. it is also a datatype of number. we can check it by using typeof operator
//console.log(typeof NaN); //number   


let score=undefined;//undefined
//console.log(score); 

let score1=null; //null
//console.log(score1);

let isLoggedIn=1; 
let booleanValue=Boolean(isLoggedIn); 
//console.log(booleanValue); //true


//1 => true
//0=>false

//""=>false  empty string m false dega boolean wale case m
//"abc"=>true string m true dega boolean wale case m 


//primitive datatypes(call by value): number, string, boolean, bigint, undefined, null, symbol

//kisi bi value ko unique bnane k liye symbol use hota h


//non-primitive datatypes(reference): object, array, function

//float ka datatype number hi h javascript m. javascript m number ka datatype float and integer dono ko represent krta h.

const sccore=100; //number
const scoreValue=100.2;//number
const name="mayank"; //string
const isLoggedIn1=true;
const outsideTemp=null;
let userEmail; //undefined

const id=Symbol("id"); //symbol
const anotherId=Symbol("id"); //symbol

//console.log(id===anotherId); //false

//because symbol is unique and primitive identifier. it will create a new symbol every time we call it. even if we pass the same description to the symbol function, it will create a new symbol. that's why id and anotherId are not equal.


//arrays, object and function

const heroes=["superman", "batman", "spiderman"]; //array
const myObj={
    name:"mayank",
    age:22,
    isLoggedIn:true
} //object

const myFunction = function(){
    //console.log("hello world");
} //function

//arrays, object and function are non-primitive datatypes. they are also called reference datatypes because they store the reference of the value in the memory. when we assign an array, object or function to a variable, we are actually storing the reference of the value in the variable. that's why when we change the value of the array, object or function, it will change the value of the variable as well.

//=====================================================================================

//memory 
//there are two types of memory in javascript: stack and heap. stack is used to store primitive datatypes and heap is used to store non-primitive datatypes. when we assign a primitive datatype to a variable, it will be stored in the stack memory. when we assign a non-primitive datatype to a variable, it will be stored in the heap memory and the reference of the value will be stored in the stack memory. that's why when we change the value of the non-primitive datatype, it will change the value of the variable as well.


let myName="mayank"; //primitive datatype
let anotherName=myName; //primitive datatype
anotherName="Peswani";

console.log(myName); //mayank
console.log(anotherName);//peswani

let userOne={
    email:"mayank@gmail.com",
    upi:"mayank@ybl"
}

let userTwo=userOne; //non-primitive datatype

userTwo.email="peswani@gmail.com";

console.log(userOne.email);
console.log(userTwo.email);
